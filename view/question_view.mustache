<main class="py-5">
    <div class="content container px-lg-5 p-4  rounded-3">
        <article class="mx-auto">
            <br>
            <div class="d-flex">
                <h3 class="col-9">Preguntas
                    {{#suggested}}sugeridas por {{/suggested}}
                    {{#repport}}reportadas por {{/repport}}
                    {{#edit}}aceptadas{{/edit}}
                    jugadores</h3>
            </div>
            <table class="tablaPreguntas table table-hover">
                <thead>
                <tr>
                    <th>Pregunta</th>
                    <th>A</th>
                    <th>B</th>
                    <th>C</th>
                    <th>D</th>
                    <th>Correcta</th>
                    <th>Tarea</th>
                </tr>
                </thead>
                <tbody>
            
                {{#questionsSuggested}}
                    <tr>
                        <td>{{descripcion}}</td>
                        <td>{{opcionA}}</td>
                        <td>{{opcionB}}</td>
                        <td>{{opcionC}}</td>
                        <td>{{opcionD}}</td>
                        <td>{{resp_correcta}}</td>
                        <td>

<div class="button-container">
    <button id="{{id}}" type="submit" name="action" value="decline" class="btn btn-danger deleteQuestion">
        <i class="fa-solid fa-xmark"></i>
    </button>

    <form action="/EditQuestion" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id" value="{{id}}">
        <button type="submit" name="action" value="edit" class="btn btn-warning">
            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
        </button>
    </form>

    <form action="/question/accept" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id" value="{{id}}">
        <button type="submit" name="action" value="accept" class="btn btn-success">
            <i class="fa fa-check" aria-hidden="true"></i>
        </button>
    </form>
</div>

                            </form>
                        </td>
                    </tr>
                {{/questionsSuggested}}
            
                {{#questionsReported}}
                    <tr>
                        <td>{{descripcion}}</td>
                        <td>{{opcionA}}</td>
                        <td>{{opcionB}}</td>
                        <td>{{opcionC}}</td>
                        <td>{{opcionD}}</td>
                        <td>{{resp_correcta}}</td>
                        <td>

 <div class="button-container">
    <button id="{{id}}" type="submit" name="action" value="decline" class="btn btn-danger deleteQuestion">
        <i class="fa-regular fa-trash-can"></i>
    </button>

    <form action="/EditQuestion" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id" value="{{id}}">
        <button type="submit" name="action" value="edit" class="btn btn-warning">
            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
        </button>
    </form>

    <form action="/question/accept" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id" value="{{id}}">
        <button type="submit" name="action" value="accept" class="btn btn-success">
            <i class="fa fa-check" aria-hidden="true"></i>
        </button>
    </form>
</div>


                        </td>
                    </tr>
                {{/questionsReported}}
            
                {{#questionsAccepted}}
                    <tr>
                        <td>{{descripcion}}</td>
                        <td>{{opcionA}}</td>
                        <td>{{opcionB}}</td>
                        <td>{{opcionC}}</td>
                        <td>{{opcionD}}</td>
                        <td>{{resp_correcta}}</td>
                        <td>

 <div class="btn-group" role="group">
    <button id="{{id}}" type="submit" name="action" value="decline" class="btn btn-danger deleteQuestion">
        <i class="fa fa-trash-o" aria-hidden="true"></i>
    </button>

    <form action="/EditQuestion" method="post" enctype="multipart/form-data" class="d-inline-block">
        <input type="hidden" name="id" value="{{id}}">
        <button type="submit" name="action" value="edit" class="btn btn-warning" style="margin-left: 10px;">
            <i class="fa-regular fa-pen-to-square"></i>
        </button>
    </form>
</div>
                        </td>
                    </tr>
                {{/questionsAccepted}}
                </tbody>
            </table>
          
            {{^questionsSuggested}}
                {{#suggested}}
                    <p class="text-center fw-bold display-5"> No hay preguntas aun</p>
                {{/suggested}}
            {{/questionsSuggested}}
            {{^questionsReported}}
                {{#repport}}
                    <p class="text-center fw-bold display-5"> No hay preguntas reportadas aun</p>
                {{/repport}}
            {{/questionsReported}}
            {{^questionsAccepted}}
                {{#edit}}
                    <p class="text-center fw-bold display-5"> No hay preguntas aceptadas disponibles aun</p>
                {{/edit}}
            {{/questionsAccepted}}
        </article>
        <div class="d-flex justify-content-center">
            <br>
            <a href="/lobby" class="btn btn-lg btn-primary m-3">Volver al lobby</a>
        </div>
    </div>

    <!--Modalees-->

    <div class="modal" id="deleteModal">
        <section class="modal-dialog">
            <article class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">Dar de Baja</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form method="get" id="deleteForm">
                    <div class="modal-body">
                        <p>¿Confirmas que quieres dar de baja esta pregunta?</p>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Cerrar</button>
                        <button type="submit" class="btn btn-success" data-bs-dismiss="modal">Confirmar</button>
                    </div>
                </form>
            </article>
        </section>
    </div>
    <div class="modal" id="newQuestionModal">
        <section class="modal-dialog">
            <article class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <p>Pregunta agregada correctamente</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Cerrar</button>
                </div>
            </article>
        </section>
    </div>
</main>
<script src="/public/js/deleteModal.js"></script>
{{#showNewQuestionModal}}
    <script src="/public/js/lobbyNewQuestionModal.js"></script>
{{/showNewQuestionModal}}
